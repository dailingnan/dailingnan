<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    <mapper namespace="com.eleme.menumanage.dao.ShowAssessDao">
    <select id="selectShow" resultType="showassess" parameterType="int">
    	select ename,tname,eaddress,img,marked from showassess where eid=#{eid}
    </select>
    
    </mapper>